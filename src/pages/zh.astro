---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { translations } from '../i18n/index';

const currentLang = 'zh';
const t = translations[currentLang];
---

<Layout title="首页 | Aivio Digital" lang={currentLang}>
  <Header currentLang={currentLang} />
  
  <main>
    <!-- Hero Section -->
    <section class="min-h-screen bg-gradient-to-br from-primary to-secondary flex items-center justify-center">
      <div class="text-center text-white px-4">
        <h1 class="text-6xl font-bold mb-6">{t.hero.title}</h1>
        <p class="text-2xl mb-8 text-blue-100">{t.hero.subtitle}</p>
        <button class="bg-white text-primary font-bold py-3 px-8 rounded-lg hover:bg-blue-50 transition text-lg">
          {t.hero.cta}
        </button>
      </div>
    </section>

    <!-- Features Section -->
    <section class="py-20 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-4xl font-bold text-center mb-12 text-gray-900">主要功能</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <div class="p-8 bg-gray-50 rounded-lg hover:shadow-lg transition">
            <div class="text-4xl mb-4">⚡</div>
            <h3 class="text-xl font-semibold mb-3">闪电般快速</h3>
            <p class="text-gray-600">采用 Astro 构建以实现最佳性能和速度。</p>
          </div>
          <div class="p-8 bg-gray-50 rounded-lg hover:shadow-lg transition">
            <div class="text-4xl mb-4">🌐</div>
            <h3 class="text-xl font-semibold mb-3">多语言支持</h3>
            <p class="text-gray-600">完整的 i18n 支持英文和中文语言。</p>
          </div>
          <div class="p-8 bg-gray-50 rounded-lg hover:shadow-lg transition">
            <div class="text-4xl mb-4">🎨</div>
            <h3 class="text-xl font-semibold mb-3">精美设计</h3>
            <p class="text-gray-600">使用 TailwindCSS 进行现代外观设计。</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="py-20 bg-primary text-white">
      <div class="max-w-4xl mx-auto text-center px-4">
        <h2 class="text-4xl font-bold mb-6">准备好开始了吗？</h2>
        <p class="text-xl mb-8 text-blue-100">加入我们，今天就创建一些了不起的东西。</p>
        <button class="bg-white text-primary font-bold py-3 px-8 rounded-lg hover:bg-blue-50 transition text-lg">
          {t.hero.cta}
        </button>
      </div>
    </section>
  </main>

  <Footer currentLang={currentLang} />
</Layout>

<style>
  main {
    width: 100%;
  }
</style>
