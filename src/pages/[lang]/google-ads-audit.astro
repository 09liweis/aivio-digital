---
import Layout from '../../layouts/Layout.astro';
import { translations } from '../../i18n';

const { lang } = Astro.params;
const currentLang = (lang || 'en') as 'en' | 'zh';
const currentPath = Astro.url.pathname;
const t = translations[currentLang].googleAdsAudit;
export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'zh' } },
  ];
}
---

<Layout title={currentLang === 'zh' ? 'Google 广告审计 | Aivio Digital' : 'Google Ads Audit | Aivio Digital'} lang={currentLang} currentLang={currentLang} currentPath={currentPath}>
  <main class="min-h-screen">
    <!-- Hero Section -->
    <section class="bg-gradient-to-br from-blue-600 to-blue-800 text-white py-20 px-4">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-4xl md:text-5xl font-bold mb-6">
          {t.hero.title}
        </h1>
        <p class="text-xl md:text-2xl text-blue-100 leading-relaxed">
          {t.hero.subtitle}
        </p>
      </div>
    </section>

    <!-- Why Audit Matters -->
    <section class="py-16 px-4 bg-white">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6 text-center">
          {t.whyAuditMatters.title}
        </h2>
        <p class="text-lg text-gray-600 mb-8 text-center">
          {t.whyAuditMatters.subtitle}
        </p>
        <ul class="space-y-4 text-gray-700">
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.whyAuditMatters.point1}</span>
          </li>
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.whyAuditMatters.point2}</span>
          </li>
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.whyAuditMatters.point3}</span>
          </li>
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.whyAuditMatters.point4}</span>
          </li>
        </ul>
        <p class="text-lg text-gray-700 mt-8 italic text-center">
          {t.whyAuditMatters.conclusion}
        </p>
      </div>
    </section>

    <!-- What We Review -->
    <section class="py-16 px-4 bg-gray-50">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-12 text-center">
          {t.whatWeReview.title}
        </h2>
        <div class="grid md:grid-cols-2 gap-6">
          <div class="bg-white p-6 rounded-lg shadow-md">
            <h3 class="text-xl font-semibold text-gray-900 mb-3">
              {t.whatWeReview.structure.title}
            </h3>
            <p class="text-gray-600">
              {t.whatWeReview.structure.description}
            </p>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-md">
            <h3 class="text-xl font-semibold text-gray-900 mb-3">
              {t.whatWeReview.keywords.title}
            </h3>
            <p class="text-gray-600">
              {t.whatWeReview.keywords.description}
            </p>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-md">
            <h3 class="text-xl font-semibold text-gray-900 mb-3">
              {t.whatWeReview.bidding.title}
            </h3>
            <p class="text-gray-600">
              {t.whatWeReview.bidding.description}
            </p>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-md">
            <h3 class="text-xl font-semibold text-gray-900 mb-3">
              {t.whatWeReview.adCopy.title}
            </h3>
            <p class="text-gray-600">
              {t.whatWeReview.adCopy.description}
            </p>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-md">
            <h3 class="text-xl font-semibold text-gray-900 mb-3">
              {t.whatWeReview.tracking.title}
            </h3>
            <p class="text-gray-600">
              {t.whatWeReview.tracking.description}
            </p>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-md">
            <h3 class="text-xl font-semibold text-gray-900 mb-3">
              {t.whatWeReview.landingPage.title}
            </h3>
            <p class="text-gray-600">
              {t.whatWeReview.landingPage.description}
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- What You Receive -->
    <section class="py-16 px-4 bg-white">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-8 text-center">
          {t.whatYouReceive.title}
        </h2>
        <ul class="space-y-4 text-gray-700">
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.whatYouReceive.point1}</span>
          </li>
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.whatYouReceive.point2}</span>
          </li>
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.whatYouReceive.point3}</span>
          </li>
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.whatYouReceive.point4}</span>
          </li>
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.whatYouReceive.point5}</span>
          </li>
        </ul>
        <p class="text-lg text-gray-600 mt-8 text-center italic">
          {t.whatYouReceive.note}
        </p>
      </div>
    </section>

    <!-- Best Fit For -->
    <section class="py-16 px-4 bg-blue-50">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-8 text-center">
          {t.bestFitFor.title}
        </h2>
        <ul class="space-y-4 text-gray-700">
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.bestFitFor.point1}</span>
          </li>
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.bestFitFor.point2}</span>
          </li>
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.bestFitFor.point3}</span>
          </li>
          <li class="flex items-start">
            <span class="text-blue-600 mr-3 mt-1">·</span>
            <span>{t.bestFitFor.point4}</span>
          </li>
        </ul>
      </div>
    </section>

    <!-- What Happens After -->
    <section class="py-16 px-4 bg-white">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6 text-center">
          {t.afterAudit.title}
        </h2>
        <p class="text-lg text-gray-600 mb-8 text-center">
          {t.afterAudit.subtitle}
        </p>
        <ol class="space-y-6 text-gray-700">
          <li class="flex items-start">
            <span class="bg-blue-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 mt-1 flex-shrink-0">1</span>
            <span class="pt-1">{t.afterAudit.option1}</span>
          </li>
          <li class="flex items-start">
            <span class="bg-blue-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 mt-1 flex-shrink-0">2</span>
            <span class="pt-1">{t.afterAudit.option2}</span>
          </li>
          <li class="flex items-start">
            <span class="bg-blue-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 mt-1 flex-shrink-0">3</span>
            <span class="pt-1">{t.afterAudit.option3}</span>
          </li>
        </ol>
        <p class="text-lg text-gray-600 mt-8 text-center italic">
          {t.afterAudit.note}
        </p>
      </div>
    </section>

    <!-- Ready CTA -->
    <section class="py-20 px-4 bg-gradient-to-br from-blue-600 to-blue-800 text-white">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-3xl md:text-4xl font-bold mb-6">
          {t.ready.title}
        </h2>
        <p class="text-xl text-blue-100 mb-8">
          {t.ready.subtitle}
        </p>
        <a
          href={`mailto:hello@aivio.digital?subject=Google Ads Audit Request`}
          class="inline-block bg-white text-blue-600 font-semibold px-8 py-4 rounded-lg hover:bg-blue-50 transition-colors text-lg"
        >
          {t.ready.button}
        </a>
      </div>
    </section>
  </main>
</Layout>

<style>
  main {
    width: 100%;
  }
</style>
